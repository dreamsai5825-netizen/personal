kind = "web"
previewPath = "/"
title = "OmniServe"
version = "1.0.0"
id = "artifacts/firebase-app"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 24814

[services.development]
run = "pnpm --filter @workspace/firebase-app run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/firebase-app", "run", "build" ]
publicDir = "artifacts/firebase-app/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "24814"
BASE_PATH = "/"
